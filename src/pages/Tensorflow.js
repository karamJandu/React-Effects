import React, { useRef } from "react";

const Tensorflow = () => {
  const imgRef = useRef();
  return (
    <div>
      <h1>Tensorflow</h1>
      <img src="" />
    </div>
  );
};

export default Tensorflow;
